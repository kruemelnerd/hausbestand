---
phase: 01-app-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/index.html
  - frontend/src/main.ts
  - frontend/src/App.vue
  - frontend/src/styles.css
  - frontend/playwright.config.ts
  - frontend/tests/e2e/home.spec.ts
  - frontend/.gitignore
autonomous: true
requirements:
  - FND-01
must_haves:
  truths:
    - "User kann die Webanwendung im Browser oeffnen und eine sichtbare Startseite sehen."
    - "User sieht auf der Startseite den Arbeitsnamen HeizInventur und klare Navigationsplatzhalter."
    - "Ein E2E-Basistest prueft den Startseitenaufruf erfolgreich."
  artifacts:
    - path: "frontend/src/App.vue"
      provides: "Startseiten-UI mit Titel und Nav-Platzhaltern"
    - path: "frontend/src/main.ts"
      provides: "Vue App Bootstrap"
    - path: "frontend/tests/e2e/home.spec.ts"
      provides: "E2E-Basistest fuer Startseite"
  key_links:
    - from: "frontend/src/main.ts"
      to: "frontend/src/App.vue"
      via: "createApp(App).mount('#app')"
      pattern: "createApp\\(App\\)"
    - from: "frontend/tests/e2e/home.spec.ts"
      to: "laufende Frontend-App"
      via: "Playwright page.goto('/')"
      pattern: "page\.goto"
---

<objective>
Eine lauffaehige mobile-first Frontend-Anwendung mit sichtbarer HeizInventur-Startseite und stabilem E2E-Basistest bereitstellen.

Purpose: Erfuellt FND-01 als belastbare Basis fuer alle Folgephasen.
Output: App-Shell-Dateien, Startseiten-Layout, Playwright-E2E-Basistest.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-skeleton/01-RESEARCH.md
@.planning/phases/01-app-skeleton/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Vue-3/Vite-App-Shell mit Startseite erstellen</name>
  <files>frontend/package.json, frontend/index.html, frontend/src/main.ts, frontend/src/App.vue, frontend/src/styles.css, frontend/.gitignore</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-app-skeleton/01-UI-SPEC.md</read_first>
  <action>Erzeuge ein Vue-3/TypeScript/Vite-Frontend unter `frontend/` mit Startseite gemaess UI-SPEC: Titel `HeizInventur` (data-testid `app-title`), sichtbarer Labeltext `Startseite`, mobile-first Layout mit `header`, `main`, `nav`, sowie drei deaktivierte Navigationsplatzhalter `Haeuser (demnaechst)`, `Grundrisse (demnaechst)`, `Reports (demnaechst)` inkl. `aria-disabled="true"` und Test-IDs `nav-placeholder-*`.</action>
  <acceptance_criteria>
    - `frontend/src/App.vue` enthaelt die Strings `HeizInventur` und `Startseite`.
    - `frontend/src/App.vue` enthaelt `data-testid="app-title"` und `data-testid="home-shell"`.
    - `frontend/src/App.vue` enthaelt alle drei Platzhalterlabels und `aria-disabled="true"`.
    - `frontend/src/main.ts` mountet Vue via `createApp(App).mount('#app')`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Frontend-App baut erfolgreich und Startseite entspricht dem UI-Contract.</done>
</task>

<task type="auto">
  <name>task 2: Playwright-E2E-Basistest fuer Startseite integrieren</name>
  <files>frontend/package.json, frontend/playwright.config.ts, frontend/tests/e2e/home.spec.ts</files>
  <read_first>frontend/package.json, frontend/src/App.vue, .planning/ROADMAP.md</read_first>
  <action>Konfiguriere Playwright fuer einen stabilen Basislauf (Chromium + mobiler Viewport) und implementiere `home.spec.ts`: oeffnet Startseite, erwartet sichtbaren Titel via `data-testid=app-title`, validiert `Startseite`-Text und mindestens einen sichtbaren Navigationsplatzhalter. Ergaenze npm-Skripte fuer `test:e2e` und ggf. `test:e2e:headed` in `frontend/package.json`.</action>
  <acceptance_criteria>
    - `frontend/tests/e2e/home.spec.ts` enthaelt `page.goto` und Assertion auf `app-title`.
    - `frontend/package.json` enthaelt Script `test:e2e` mit Playwright-Aufruf.
    - `frontend/playwright.config.ts` ist vorhanden und referenziert `tests/e2e`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run test:e2e</automated>
  </verify>
  <done>E2E-Basistest ist stabil lauffaehig und deckt das Success-Criterion 3 ab.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Frontend Runtime | Untrusted client environment renders UI and can manipulate local state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | App shell markup | mitigate | Keep placeholder navigation non-functional (`aria-disabled`, no active routes) to avoid accidental unsafe navigation assumptions. |
| T-01-02 | I (Information Disclosure) | Start page content | accept | Phase 1 displays no sensitive data and no backend integration; low-risk acceptance documented. |
</threat_model>

<verification>
- `cd frontend && npm run build`
- `cd frontend && npm run test:e2e`
</verification>

<success_criteria>
- Anwendung startet lokal und zeigt sichtbare HeizInventur-Startseite.
- Startseite enthaelt Arbeitsnamen und Navigationsplatzhalter gemaess UI-SPEC.
- E2E-Basistest fuer Startseitenaufruf ist gruen.
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-skeleton/01-app-skeleton-01-SUMMARY.md`
</output>
